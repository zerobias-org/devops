/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { Entry, Group, GroupInfo, GroupZone, GroupZoneGroup, User } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace GroupApi {
}

/**
 * GroupApi - interface
 * @export
 * @interface GroupApi
 */
export interface GroupApi {
  /**
   * 
   * @summary Retrieves single access group details by group ID
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} groupId Unique identifier for a group
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.get('organizationId', 'groupId');
   * ```
   * @openapi operation=getGroup path=/organizations/{organizationId}/groups/{groupId} method=GET
   * @memberof GroupApi
   */
  get(organizationId: string, groupId: string): Promise<GroupInfo>;

  /**
   * 
   * @summary Retrieves all entries/permissions for a specific group
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} groupId Unique identifier for a group
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listEntries('organizationId', 'groupId', 1, 1);
   * ```
   * @openapi operation=listGroupEntries path=/organizations/{organizationId}/groups/{groupId}/entries method=GET
   * @memberof GroupApi
   */
  listEntries(organizationId: string, groupId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<Entry>>;

  /**
   * 
   * @summary Retrieves all users belonging to a specific group
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} groupId Unique identifier for a group
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listUsers('organizationId', 'groupId', 1, 1);
   * ```
   * @openapi operation=listGroupUsers path=/organizations/{organizationId}/groups/{groupId}/users method=GET
   * @memberof GroupApi
   */
  listUsers(organizationId: string, groupId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<User>>;

  /**
   * 
   * @summary Retrieves all zones and their associated configurations for a specific group
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} groupId Unique identifier for a group
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listZoneGroups('organizationId', 'groupId', 1, 1);
   * ```
   * @openapi operation=listGroupZoneGroups path=/organizations/{organizationId}/groups/{groupId}/zoneGroups method=GET
   * @memberof GroupApi
   */
  listZoneGroups(organizationId: string, groupId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<GroupZoneGroup>>;

  /**
   * 
   * @summary Retrieves all zones for a specific group
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} groupId Unique identifier for a group
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listZones('organizationId', 'groupId', 1, 1);
   * ```
   * @openapi operation=listGroupZones path=/organizations/{organizationId}/groups/{groupId}/zones method=GET
   * @memberof GroupApi
   */
  listZones(organizationId: string, groupId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<GroupZone>>;

  /**
   * 
   * @summary Retrieves all access groups in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.list('organizationId', 1, 1);
   * ```
   * @openapi operation=listGroups path=/organizations/{organizationId}/groups method=GET
   * @memberof GroupApi
   */
  list(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<Group>>;

}

/**
 * GroupProducerApi - interface
 * Producer interface for Group
 * @export
 * @interface GroupProducerApi
 */
export interface GroupProducerApi {
  /**
  * 
  * @summary Retrieves single access group details by group ID
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} groupId Unique identifier for a group
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getGroup path=/organizations/{organizationId}/groups/{groupId} method=GET
  * @memberof Group
  */
  get(organizationId: string, groupId: string): Promise<GroupInfo>;
  /**
  * 
  * @summary Retrieves all entries/permissions for a specific group
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} groupId Unique identifier for a group
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listGroupEntries path=/organizations/{organizationId}/groups/{groupId}/entries method=GET
  * @memberof Group
  */
  listEntries(results: PagedResults<Entry>, organizationId: string, groupId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all users belonging to a specific group
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} groupId Unique identifier for a group
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listGroupUsers path=/organizations/{organizationId}/groups/{groupId}/users method=GET
  * @memberof Group
  */
  listUsers(results: PagedResults<User>, organizationId: string, groupId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all zones and their associated configurations for a specific group
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} groupId Unique identifier for a group
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listGroupZoneGroups path=/organizations/{organizationId}/groups/{groupId}/zoneGroups method=GET
  * @memberof Group
  */
  listZoneGroups(results: PagedResults<GroupZoneGroup>, organizationId: string, groupId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all zones for a specific group
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} groupId Unique identifier for a group
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listGroupZones path=/organizations/{organizationId}/groups/{groupId}/zones method=GET
  * @memberof Group
  */
  listZones(results: PagedResults<GroupZone>, organizationId: string, groupId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all access groups in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listGroups path=/organizations/{organizationId}/groups method=GET
  * @memberof Group
  */
  list(results: PagedResults<Group>, organizationId: string): Promise<void>;
}



export class GroupApiMappingImpl implements GroupApi {
  private producer: GroupProducerApi;
  constructor(producer: GroupProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Retrieves single access group details by group ID
   * @param organizationId Unique identifier for the organization
   * @param groupId Unique identifier for a group
   * @openapi operation=getGroup path=/organizations/{organizationId}/groups/{groupId} method=GET
   */
  public async get(organizationId: string, groupId: string): Promise<GroupInfo> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'groupId' is not null or undefined
    if (groupId === null || groupId === undefined || groupId === '') {
      throw new ParameterRequiredError('groupId');
    }
    return this.producer.get(organizationId, groupId);
  }
  /**
   * 
   * @summary Retrieves all entries/permissions for a specific group
   * @param organizationId Unique identifier for the organization
   * @param groupId Unique identifier for a group
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listGroupEntries path=/organizations/{organizationId}/groups/{groupId}/entries method=GET
   */
  public async listEntries(organizationId: string, groupId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<Entry>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'groupId' is not null or undefined
    if (groupId === null || groupId === undefined || groupId === '') {
      throw new ParameterRequiredError('groupId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Entry>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listEntries(bag, organizationId, groupId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all users belonging to a specific group
   * @param organizationId Unique identifier for the organization
   * @param groupId Unique identifier for a group
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listGroupUsers path=/organizations/{organizationId}/groups/{groupId}/users method=GET
   */
  public async listUsers(organizationId: string, groupId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<User>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'groupId' is not null or undefined
    if (groupId === null || groupId === undefined || groupId === '') {
      throw new ParameterRequiredError('groupId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<User>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listUsers(bag, organizationId, groupId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all zones and their associated configurations for a specific group
   * @param organizationId Unique identifier for the organization
   * @param groupId Unique identifier for a group
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listGroupZoneGroups path=/organizations/{organizationId}/groups/{groupId}/zoneGroups method=GET
   */
  public async listZoneGroups(organizationId: string, groupId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<GroupZoneGroup>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'groupId' is not null or undefined
    if (groupId === null || groupId === undefined || groupId === '') {
      throw new ParameterRequiredError('groupId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<GroupZoneGroup>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listZoneGroups(bag, organizationId, groupId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all zones for a specific group
   * @param organizationId Unique identifier for the organization
   * @param groupId Unique identifier for a group
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listGroupZones path=/organizations/{organizationId}/groups/{groupId}/zones method=GET
   */
  public async listZones(organizationId: string, groupId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<GroupZone>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'groupId' is not null or undefined
    if (groupId === null || groupId === undefined || groupId === '') {
      throw new ParameterRequiredError('groupId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<GroupZone>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listZones(bag, organizationId, groupId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all access groups in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listGroups path=/organizations/{organizationId}/groups method=GET
   */
  public async list(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<Group>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Group>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.list(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}


export class GroupApiHubImpl implements GroupProducerApi {
  private client: AxiosInstance;

  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async get(organizationId: string, groupId: string): Promise<GroupInfo> {
  const _body = {
      organizationId,groupId,
  };
  return this.client.put(
    '/getGroup',
    {
      ..._body,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      return ObjectSerializer.deserialize(response.data, 'GroupInfo')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listEntries(results: PagedResults<Entry>, organizationId: string, groupId: string): Promise<void> {
  const _body = {
      organizationId,groupId,
  };
  return this.client.put(
    '/listGroupEntries',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Entry>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listGroupEntries`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Entry');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listUsers(results: PagedResults<User>, organizationId: string, groupId: string): Promise<void> {
  const _body = {
      organizationId,groupId,
  };
  return this.client.put(
    '/listGroupUsers',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<User>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listGroupUsers`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'User');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listZoneGroups(results: PagedResults<GroupZoneGroup>, organizationId: string, groupId: string): Promise<void> {
  const _body = {
      organizationId,groupId,
  };
  return this.client.put(
    '/listGroupZoneGroups',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<GroupZoneGroup>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listGroupZoneGroups`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'GroupZoneGroup');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listZones(results: PagedResults<GroupZone>, organizationId: string, groupId: string): Promise<void> {
  const _body = {
      organizationId,groupId,
  };
  return this.client.put(
    '/listGroupZones',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<GroupZone>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listGroupZones`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'GroupZone');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async list(results: PagedResults<Group>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listGroups',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Group>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listGroups`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Group');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}
