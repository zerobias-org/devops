/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { Schedule, ScheduleEvent, ScheduleType } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace ScheduleApi {
}

/**
 * ScheduleApi - interface
 * @export
 * @interface ScheduleApi
 */
export interface ScheduleApi {
  /**
   * 
   * @summary Retrieves all events for a specific schedule
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} scheduleId Unique identifier for a schedule
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listScheduleEvents('organizationId', 'scheduleId', 1, 1);
   * ```
   * @openapi operation=listScheduleEvents path=/organizations/{organizationId}/schedules/{scheduleId}/events method=GET
   * @memberof ScheduleApi
   */
  listScheduleEvents(organizationId: string, scheduleId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<ScheduleEvent>>;

  /**
   * 
   * @summary Retrieves all schedule types in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listScheduleTypes('organizationId', 1, 1);
   * ```
   * @openapi operation=listScheduleTypes path=/organizations/{organizationId}/scheduleTypes method=GET
   * @memberof ScheduleApi
   */
  listScheduleTypes(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<ScheduleType>>;

  /**
   * 
   * @summary Retrieves all schedules in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listSchedules('organizationId', 1, 1);
   * ```
   * @openapi operation=listSchedules path=/organizations/{organizationId}/schedules method=GET
   * @memberof ScheduleApi
   */
  listSchedules(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<Schedule>>;

}

/**
 * ScheduleProducerApi - interface
 * Producer interface for Schedule
 * @export
 * @interface ScheduleProducerApi
 */
export interface ScheduleProducerApi {
  /**
  * 
  * @summary Retrieves all events for a specific schedule
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} scheduleId Unique identifier for a schedule
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listScheduleEvents path=/organizations/{organizationId}/schedules/{scheduleId}/events method=GET
  * @memberof Schedule
  */
  listScheduleEvents(results: PagedResults<ScheduleEvent>, organizationId: string, scheduleId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all schedule types in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listScheduleTypes path=/organizations/{organizationId}/scheduleTypes method=GET
  * @memberof Schedule
  */
  listScheduleTypes(results: PagedResults<ScheduleType>, organizationId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all schedules in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listSchedules path=/organizations/{organizationId}/schedules method=GET
  * @memberof Schedule
  */
  listSchedules(results: PagedResults<Schedule>, organizationId: string): Promise<void>;
}



export class ScheduleApiMappingImpl implements ScheduleApi {
  private producer: ScheduleProducerApi;
  constructor(producer: ScheduleProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Retrieves all events for a specific schedule
   * @param organizationId Unique identifier for the organization
   * @param scheduleId Unique identifier for a schedule
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listScheduleEvents path=/organizations/{organizationId}/schedules/{scheduleId}/events method=GET
   */
  public async listScheduleEvents(organizationId: string, scheduleId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<ScheduleEvent>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'scheduleId' is not null or undefined
    if (scheduleId === null || scheduleId === undefined || scheduleId === '') {
      throw new ParameterRequiredError('scheduleId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<ScheduleEvent>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listScheduleEvents(bag, organizationId, scheduleId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all schedule types in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listScheduleTypes path=/organizations/{organizationId}/scheduleTypes method=GET
   */
  public async listScheduleTypes(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<ScheduleType>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<ScheduleType>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listScheduleTypes(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all schedules in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listSchedules path=/organizations/{organizationId}/schedules method=GET
   */
  public async listSchedules(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<Schedule>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Schedule>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listSchedules(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}


export class ScheduleApiHubImpl implements ScheduleProducerApi {
  private client: AxiosInstance;

  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async listScheduleEvents(results: PagedResults<ScheduleEvent>, organizationId: string, scheduleId: string): Promise<void> {
  const _body = {
      organizationId,scheduleId,
  };
  return this.client.put(
    '/listScheduleEvents',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<ScheduleEvent>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listScheduleEvents`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'ScheduleEvent');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listScheduleTypes(results: PagedResults<ScheduleType>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listScheduleTypes',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<ScheduleType>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listScheduleTypes`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'ScheduleType');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listSchedules(results: PagedResults<Schedule>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listSchedules',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Schedule>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listSchedules`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Schedule');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}
