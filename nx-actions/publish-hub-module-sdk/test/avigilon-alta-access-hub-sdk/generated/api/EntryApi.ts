/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { Entry, EntryActivityEvent, EntryCamera, EntryInfo, EntryStateInfo, EntryUser, EntryUserSchedule } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace EntryApi {
}

/**
 * EntryApi - interface
 * @export
 * @interface EntryApi
 */
export interface EntryApi {
  /**
   * 
   * @summary Retrieves detailed information for a specific entry
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} entryId Unique identifier for an entry
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.get('organizationId', 'entryId');
   * ```
   * @openapi operation=getEntry path=/organizations/{organizationId}/entries/{entryId} method=GET
   * @memberof EntryApi
   */
  get(organizationId: string, entryId: string): Promise<EntryInfo>;

  /**
   * 
   * @summary Retrieves all entries in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.list('organizationId', 1, 1);
   * ```
   * @openapi operation=listEntries path=/organizations/{organizationId}/entries method=GET
   * @memberof EntryApi
   */
  list(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<Entry>>;

  /**
   * 
   * @summary Retrieves activity report for a specific entry
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} entryId Unique identifier for an entry
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listActivity('organizationId', 'entryId', 1, 1);
   * ```
   * @openapi operation=listEntryActivity path=/organizations/{organizationId}/entries/{entryId}/activity method=GET
   * @memberof EntryApi
   */
  listActivity(organizationId: string, entryId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<EntryActivityEvent>>;

  /**
   * 
   * @summary Retrieves all cameras associated with a specific entry
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} entryId Unique identifier for an entry
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listCameras('organizationId', 'entryId', 1, 1);
   * ```
   * @openapi operation=listEntryCameras path=/organizations/{organizationId}/entries/{entryId}/cameras method=GET
   * @memberof EntryApi
   */
  listCameras(organizationId: string, entryId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<EntryCamera>>;

  /**
   * 
   * @summary Retrieves all entry states in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listEntryStates('organizationId', 1, 1);
   * ```
   * @openapi operation=listEntryStates path=/organizations/{organizationId}/entryStates method=GET
   * @memberof EntryApi
   */
  listEntryStates(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<EntryStateInfo>>;

  /**
   * 
   * @summary Retrieves all active users with their associated schedules that have access to a specific entry
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} entryId Unique identifier for an entry
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listUserSchedules('organizationId', 'entryId', 1, 1);
   * ```
   * @openapi operation=listEntryUserSchedules path=/organizations/{organizationId}/entries/{entryId}/userSchedules method=GET
   * @memberof EntryApi
   */
  listUserSchedules(organizationId: string, entryId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<EntryUserSchedule>>;

  /**
   * 
   * @summary Retrieves all active users that have access to a specific entry
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} entryId Unique identifier for an entry
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listUsers('organizationId', 'entryId', 1, 1);
   * ```
   * @openapi operation=listEntryUsers path=/organizations/{organizationId}/entries/{entryId}/users method=GET
   * @memberof EntryApi
   */
  listUsers(organizationId: string, entryId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<EntryUser>>;

}

/**
 * EntryProducerApi - interface
 * Producer interface for Entry
 * @export
 * @interface EntryProducerApi
 */
export interface EntryProducerApi {
  /**
  * 
  * @summary Retrieves detailed information for a specific entry
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} entryId Unique identifier for an entry
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getEntry path=/organizations/{organizationId}/entries/{entryId} method=GET
  * @memberof Entry
  */
  get(organizationId: string, entryId: string): Promise<EntryInfo>;
  /**
  * 
  * @summary Retrieves all entries in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEntries path=/organizations/{organizationId}/entries method=GET
  * @memberof Entry
  */
  list(results: PagedResults<Entry>, organizationId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves activity report for a specific entry
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} entryId Unique identifier for an entry
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEntryActivity path=/organizations/{organizationId}/entries/{entryId}/activity method=GET
  * @memberof Entry
  */
  listActivity(results: PagedResults<EntryActivityEvent>, organizationId: string, entryId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all cameras associated with a specific entry
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} entryId Unique identifier for an entry
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEntryCameras path=/organizations/{organizationId}/entries/{entryId}/cameras method=GET
  * @memberof Entry
  */
  listCameras(results: PagedResults<EntryCamera>, organizationId: string, entryId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all entry states in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEntryStates path=/organizations/{organizationId}/entryStates method=GET
  * @memberof Entry
  */
  listEntryStates(results: PagedResults<EntryStateInfo>, organizationId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all active users with their associated schedules that have access to a specific entry
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} entryId Unique identifier for an entry
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEntryUserSchedules path=/organizations/{organizationId}/entries/{entryId}/userSchedules method=GET
  * @memberof Entry
  */
  listUserSchedules(results: PagedResults<EntryUserSchedule>, organizationId: string, entryId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all active users that have access to a specific entry
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} entryId Unique identifier for an entry
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEntryUsers path=/organizations/{organizationId}/entries/{entryId}/users method=GET
  * @memberof Entry
  */
  listUsers(results: PagedResults<EntryUser>, organizationId: string, entryId: string): Promise<void>;
}



export class EntryApiMappingImpl implements EntryApi {
  private producer: EntryProducerApi;
  constructor(producer: EntryProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Retrieves detailed information for a specific entry
   * @param organizationId Unique identifier for the organization
   * @param entryId Unique identifier for an entry
   * @openapi operation=getEntry path=/organizations/{organizationId}/entries/{entryId} method=GET
   */
  public async get(organizationId: string, entryId: string): Promise<EntryInfo> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'entryId' is not null or undefined
    if (entryId === null || entryId === undefined || entryId === '') {
      throw new ParameterRequiredError('entryId');
    }
    return this.producer.get(organizationId, entryId);
  }
  /**
   * 
   * @summary Retrieves all entries in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listEntries path=/organizations/{organizationId}/entries method=GET
   */
  public async list(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<Entry>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Entry>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.list(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves activity report for a specific entry
   * @param organizationId Unique identifier for the organization
   * @param entryId Unique identifier for an entry
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listEntryActivity path=/organizations/{organizationId}/entries/{entryId}/activity method=GET
   */
  public async listActivity(organizationId: string, entryId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<EntryActivityEvent>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'entryId' is not null or undefined
    if (entryId === null || entryId === undefined || entryId === '') {
      throw new ParameterRequiredError('entryId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<EntryActivityEvent>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listActivity(bag, organizationId, entryId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all cameras associated with a specific entry
   * @param organizationId Unique identifier for the organization
   * @param entryId Unique identifier for an entry
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listEntryCameras path=/organizations/{organizationId}/entries/{entryId}/cameras method=GET
   */
  public async listCameras(organizationId: string, entryId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<EntryCamera>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'entryId' is not null or undefined
    if (entryId === null || entryId === undefined || entryId === '') {
      throw new ParameterRequiredError('entryId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<EntryCamera>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listCameras(bag, organizationId, entryId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all entry states in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listEntryStates path=/organizations/{organizationId}/entryStates method=GET
   */
  public async listEntryStates(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<EntryStateInfo>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<EntryStateInfo>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listEntryStates(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all active users with their associated schedules that have access to a specific entry
   * @param organizationId Unique identifier for the organization
   * @param entryId Unique identifier for an entry
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listEntryUserSchedules path=/organizations/{organizationId}/entries/{entryId}/userSchedules method=GET
   */
  public async listUserSchedules(organizationId: string, entryId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<EntryUserSchedule>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'entryId' is not null or undefined
    if (entryId === null || entryId === undefined || entryId === '') {
      throw new ParameterRequiredError('entryId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<EntryUserSchedule>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listUserSchedules(bag, organizationId, entryId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all active users that have access to a specific entry
   * @param organizationId Unique identifier for the organization
   * @param entryId Unique identifier for an entry
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listEntryUsers path=/organizations/{organizationId}/entries/{entryId}/users method=GET
   */
  public async listUsers(organizationId: string, entryId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<EntryUser>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'entryId' is not null or undefined
    if (entryId === null || entryId === undefined || entryId === '') {
      throw new ParameterRequiredError('entryId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<EntryUser>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listUsers(bag, organizationId, entryId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}


export class EntryApiHubImpl implements EntryProducerApi {
  private client: AxiosInstance;

  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async get(organizationId: string, entryId: string): Promise<EntryInfo> {
  const _body = {
      organizationId,entryId,
  };
  return this.client.put(
    '/getEntry',
    {
      ..._body,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      return ObjectSerializer.deserialize(response.data, 'EntryInfo')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async list(results: PagedResults<Entry>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listEntries',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Entry>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listEntries`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Entry');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listActivity(results: PagedResults<EntryActivityEvent>, organizationId: string, entryId: string): Promise<void> {
  const _body = {
      organizationId,entryId,
  };
  return this.client.put(
    '/listEntryActivity',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<EntryActivityEvent>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listEntryActivity`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'EntryActivityEvent');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listCameras(results: PagedResults<EntryCamera>, organizationId: string, entryId: string): Promise<void> {
  const _body = {
      organizationId,entryId,
  };
  return this.client.put(
    '/listEntryCameras',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<EntryCamera>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listEntryCameras`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'EntryCamera');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listEntryStates(results: PagedResults<EntryStateInfo>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listEntryStates',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<EntryStateInfo>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listEntryStates`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'EntryStateInfo');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listUserSchedules(results: PagedResults<EntryUserSchedule>, organizationId: string, entryId: string): Promise<void> {
  const _body = {
      organizationId,entryId,
  };
  return this.client.put(
    '/listEntryUserSchedules',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<EntryUserSchedule>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listEntryUserSchedules`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'EntryUserSchedule');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listUsers(results: PagedResults<EntryUser>, organizationId: string, entryId: string): Promise<void> {
  const _body = {
      organizationId,entryId,
  };
  return this.client.put(
    '/listEntryUsers',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<EntryUser>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listEntryUsers`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'EntryUser');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}
