/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { RoleInfo, RoleUser } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace RoleApi {
}

/**
 * RoleApi - interface
 * @export
 * @interface RoleApi
 */
export interface RoleApi {
  /**
   * 
   * @summary Retrieves all users assigned to a specific role
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} roleId Unique identifier for a role
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listRoleUsers('organizationId', 'roleId', 1, 1);
   * ```
   * @openapi operation=listRoleUsers path=/organizations/{organizationId}/roles/{roleId}/users method=GET
   * @memberof RoleApi
   */
  listRoleUsers(organizationId: string, roleId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<RoleUser>>;

  /**
   * 
   * @summary Retrieves all roles in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listRoles('organizationId', 1, 1);
   * ```
   * @openapi operation=listRoles path=/organizations/{organizationId}/roles method=GET
   * @memberof RoleApi
   */
  listRoles(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<RoleInfo>>;

}

/**
 * RoleProducerApi - interface
 * Producer interface for Role
 * @export
 * @interface RoleProducerApi
 */
export interface RoleProducerApi {
  /**
  * 
  * @summary Retrieves all users assigned to a specific role
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} roleId Unique identifier for a role
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listRoleUsers path=/organizations/{organizationId}/roles/{roleId}/users method=GET
  * @memberof Role
  */
  listRoleUsers(results: PagedResults<RoleUser>, organizationId: string, roleId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all roles in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listRoles path=/organizations/{organizationId}/roles method=GET
  * @memberof Role
  */
  listRoles(results: PagedResults<RoleInfo>, organizationId: string): Promise<void>;
}



export class RoleApiMappingImpl implements RoleApi {
  private producer: RoleProducerApi;
  constructor(producer: RoleProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Retrieves all users assigned to a specific role
   * @param organizationId Unique identifier for the organization
   * @param roleId Unique identifier for a role
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listRoleUsers path=/organizations/{organizationId}/roles/{roleId}/users method=GET
   */
  public async listRoleUsers(organizationId: string, roleId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<RoleUser>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'roleId' is not null or undefined
    if (roleId === null || roleId === undefined || roleId === '') {
      throw new ParameterRequiredError('roleId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<RoleUser>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listRoleUsers(bag, organizationId, roleId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all roles in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listRoles path=/organizations/{organizationId}/roles method=GET
   */
  public async listRoles(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<RoleInfo>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<RoleInfo>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listRoles(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}


export class RoleApiHubImpl implements RoleProducerApi {
  private client: AxiosInstance;

  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async listRoleUsers(results: PagedResults<RoleUser>, organizationId: string, roleId: string): Promise<void> {
  const _body = {
      organizationId,roleId,
  };
  return this.client.put(
    '/listRoleUsers',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<RoleUser>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listRoleUsers`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'RoleUser');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listRoles(results: PagedResults<RoleInfo>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listRoles',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<RoleInfo>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listRoles`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'RoleInfo');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}
