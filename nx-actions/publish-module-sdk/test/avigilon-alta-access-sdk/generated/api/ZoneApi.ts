/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { Zone, ZoneShare, ZoneZoneUser } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace ZoneApi {
}

/**
 * ZoneApi - interface
 * @export
 * @interface ZoneApi
 */
export interface ZoneApi {
  /**
   * 
   * @summary Retrieves all zone shares for a specific zone
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} zoneId Unique identifier for a zone
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listZoneShares('organizationId', 'zoneId', 1, 1);
   * ```
   * @openapi operation=listZoneShares path=/organizations/{organizationId}/zones/{zoneId}/zoneShares method=GET
   * @memberof ZoneApi
   */
  listZoneShares(organizationId: string, zoneId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<ZoneShare>>;

  /**
   * 
   * @summary Retrieves all zone users with their access configurations for a specific zone
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} zoneId Unique identifier for a zone
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listZoneUsers('organizationId', 'zoneId', 1, 1);
   * ```
   * @openapi operation=listZoneZoneUsers path=/organizations/{organizationId}/zones/{zoneId}/zoneUsers method=GET
   * @memberof ZoneApi
   */
  listZoneUsers(organizationId: string, zoneId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<ZoneZoneUser>>;

  /**
   * 
   * @summary Retrieves all zones in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.list('organizationId', 1, 1);
   * ```
   * @openapi operation=listZones path=/organizations/{organizationId}/zones method=GET
   * @memberof ZoneApi
   */
  list(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<Zone>>;

}

/**
 * ZoneProducerApi - interface
 * Producer interface for Zone
 * @export
 * @interface ZoneProducerApi
 */
export interface ZoneProducerApi {
  /**
  * 
  * @summary Retrieves all zone shares for a specific zone
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} zoneId Unique identifier for a zone
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listZoneShares path=/organizations/{organizationId}/zones/{zoneId}/zoneShares method=GET
  * @memberof Zone
  */
  listZoneShares(results: PagedResults<ZoneShare>, organizationId: string, zoneId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all zone users with their access configurations for a specific zone
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} zoneId Unique identifier for a zone
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listZoneZoneUsers path=/organizations/{organizationId}/zones/{zoneId}/zoneUsers method=GET
  * @memberof Zone
  */
  listZoneUsers(results: PagedResults<ZoneZoneUser>, organizationId: string, zoneId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all zones in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listZones path=/organizations/{organizationId}/zones method=GET
  * @memberof Zone
  */
  list(results: PagedResults<Zone>, organizationId: string): Promise<void>;
}

export class ZoneApiMappingImpl implements ZoneApi {
  private producer: ZoneProducerApi;
  constructor(producer: ZoneProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Retrieves all zone shares for a specific zone
   * @param organizationId Unique identifier for the organization
   * @param zoneId Unique identifier for a zone
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listZoneShares path=/organizations/{organizationId}/zones/{zoneId}/zoneShares method=GET
   */
  public async listZoneShares(organizationId: string, zoneId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<ZoneShare>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'zoneId' is not null or undefined
    if (zoneId === null || zoneId === undefined || zoneId === '') {
      throw new ParameterRequiredError('zoneId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<ZoneShare>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listZoneShares(bag, organizationId, zoneId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all zone users with their access configurations for a specific zone
   * @param organizationId Unique identifier for the organization
   * @param zoneId Unique identifier for a zone
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listZoneZoneUsers path=/organizations/{organizationId}/zones/{zoneId}/zoneUsers method=GET
   */
  public async listZoneUsers(organizationId: string, zoneId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<ZoneZoneUser>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'zoneId' is not null or undefined
    if (zoneId === null || zoneId === undefined || zoneId === '') {
      throw new ParameterRequiredError('zoneId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<ZoneZoneUser>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listZoneUsers(bag, organizationId, zoneId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all zones in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listZones path=/organizations/{organizationId}/zones method=GET
   */
  public async list(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<Zone>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Zone>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.list(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapZoneProducer(producer: ZoneProducerApi): ZoneApi {
  return new ZoneApiMappingImpl(producer);
}

/**
 * ZoneApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class ZoneApiHttpImpl implements ZoneProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async listZoneShares(results: PagedResults<ZoneShare>, organizationId: string, zoneId: string): Promise<void> {
  const _body = {
      organizationId,zoneId,
  };
  let _path = '/organizations/{organizationId}/zones/{zoneId}/zoneShares';
  _path = _path.replace('{organizationId}', encodeURIComponent(String(organizationId)));
  _path = _path.replace('{zoneId}', encodeURIComponent(String(zoneId)));
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<ZoneShare>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/organizations/{organizationId}/zones/{zoneId}/zoneShares`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'ZoneShare');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listZoneUsers(results: PagedResults<ZoneZoneUser>, organizationId: string, zoneId: string): Promise<void> {
  const _body = {
      organizationId,zoneId,
  };
  let _path = '/organizations/{organizationId}/zones/{zoneId}/zoneUsers';
  _path = _path.replace('{organizationId}', encodeURIComponent(String(organizationId)));
  _path = _path.replace('{zoneId}', encodeURIComponent(String(zoneId)));
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<ZoneZoneUser>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/organizations/{organizationId}/zones/{zoneId}/zoneUsers`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'ZoneZoneUser');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async list(results: PagedResults<Zone>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  let _path = '/organizations/{organizationId}/zones';
  _path = _path.replace('{organizationId}', encodeURIComponent(String(organizationId)));
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Zone>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/organizations/{organizationId}/zones`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Zone');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}
