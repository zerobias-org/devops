/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { CardFormat, CredentialAction, CredentialActionType, CredentialType, OrgCredential } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace CredentialApi {
}

/**
 * CredentialApi - interface
 * @export
 * @interface CredentialApi
 */
export interface CredentialApi {
  /**
   * 
   * @summary Retrieves all card formats in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listCardFormats('organizationId', 1, 1);
   * ```
   * @openapi operation=listCardFormats path=/organizations/{organizationId}/cardFormats method=GET
   * @memberof CredentialApi
   */
  listCardFormats(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<CardFormat>>;

  /**
   * 
   * @summary Retrieves all credential action types in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listCredentialActionTypes('organizationId', 1, 1);
   * ```
   * @openapi operation=listCredentialActionTypes path=/organizations/{organizationId}/credentialActionTypes method=GET
   * @memberof CredentialApi
   */
  listCredentialActionTypes(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<CredentialActionType>>;

  /**
   * 
   * @summary Retrieves all actions performed on a specific credential
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} credentialId Unique identifier for a credential
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listCredentialActions('organizationId', 'credentialId', 1, 1);
   * ```
   * @openapi operation=listCredentialActions path=/organizations/{organizationId}/credentials/{credentialId}/credentialActions method=GET
   * @memberof CredentialApi
   */
  listCredentialActions(organizationId: string, credentialId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<CredentialAction>>;

  /**
   * 
   * @summary Retrieves all credential types in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listCredentialTypes('organizationId', 1, 1);
   * ```
   * @openapi operation=listCredentialTypes path=/organizations/{organizationId}/credentialTypes method=GET
   * @memberof CredentialApi
   */
  listCredentialTypes(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<CredentialType>>;

  /**
   * 
   * @summary Retrieves all credentials in the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listOrgCredentials('organizationId', 1, 1);
   * ```
   * @openapi operation=listOrgCredentials path=/organizations/{organizationId}/credentials method=GET
   * @memberof CredentialApi
   */
  listOrgCredentials(organizationId: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<OrgCredential>>;

}

/**
 * CredentialProducerApi - interface
 * Producer interface for Credential
 * @export
 * @interface CredentialProducerApi
 */
export interface CredentialProducerApi {
  /**
  * 
  * @summary Retrieves all card formats in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listCardFormats path=/organizations/{organizationId}/cardFormats method=GET
  * @memberof Credential
  */
  listCardFormats(results: PagedResults<CardFormat>, organizationId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all credential action types in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listCredentialActionTypes path=/organizations/{organizationId}/credentialActionTypes method=GET
  * @memberof Credential
  */
  listCredentialActionTypes(results: PagedResults<CredentialActionType>, organizationId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all actions performed on a specific credential
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} credentialId Unique identifier for a credential
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listCredentialActions path=/organizations/{organizationId}/credentials/{credentialId}/credentialActions method=GET
  * @memberof Credential
  */
  listCredentialActions(results: PagedResults<CredentialAction>, organizationId: string, credentialId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all credential types in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listCredentialTypes path=/organizations/{organizationId}/credentialTypes method=GET
  * @memberof Credential
  */
  listCredentialTypes(results: PagedResults<CredentialType>, organizationId: string): Promise<void>;
  /**
  * 
  * @summary Retrieves all credentials in the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listOrgCredentials path=/organizations/{organizationId}/credentials method=GET
  * @memberof Credential
  */
  listOrgCredentials(results: PagedResults<OrgCredential>, organizationId: string): Promise<void>;
}



export class CredentialApiMappingImpl implements CredentialApi {
  private producer: CredentialProducerApi;
  constructor(producer: CredentialProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Retrieves all card formats in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listCardFormats path=/organizations/{organizationId}/cardFormats method=GET
   */
  public async listCardFormats(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<CardFormat>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<CardFormat>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listCardFormats(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all credential action types in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listCredentialActionTypes path=/organizations/{organizationId}/credentialActionTypes method=GET
   */
  public async listCredentialActionTypes(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<CredentialActionType>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<CredentialActionType>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listCredentialActionTypes(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all actions performed on a specific credential
   * @param organizationId Unique identifier for the organization
   * @param credentialId Unique identifier for a credential
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listCredentialActions path=/organizations/{organizationId}/credentials/{credentialId}/credentialActions method=GET
   */
  public async listCredentialActions(organizationId: string, credentialId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<CredentialAction>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    // verify required parameter 'credentialId' is not null or undefined
    if (credentialId === null || credentialId === undefined || credentialId === '') {
      throw new ParameterRequiredError('credentialId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<CredentialAction>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listCredentialActions(bag, organizationId, credentialId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all credential types in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listCredentialTypes path=/organizations/{organizationId}/credentialTypes method=GET
   */
  public async listCredentialTypes(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<CredentialType>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<CredentialType>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listCredentialTypes(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Retrieves all credentials in the organization
   * @param organizationId Unique identifier for the organization
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listOrgCredentials path=/organizations/{organizationId}/credentials method=GET
   */
  public async listOrgCredentials(organizationId: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<OrgCredential>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<OrgCredential>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listOrgCredentials(bag, organizationId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}


export class CredentialApiHubImpl implements CredentialProducerApi {
  private client: AxiosInstance;

  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async listCardFormats(results: PagedResults<CardFormat>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listCardFormats',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<CardFormat>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listCardFormats`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'CardFormat');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listCredentialActionTypes(results: PagedResults<CredentialActionType>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listCredentialActionTypes',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<CredentialActionType>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listCredentialActionTypes`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'CredentialActionType');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listCredentialActions(results: PagedResults<CredentialAction>, organizationId: string, credentialId: string): Promise<void> {
  const _body = {
      organizationId,credentialId,
  };
  return this.client.put(
    '/listCredentialActions',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<CredentialAction>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listCredentialActions`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'CredentialAction');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listCredentialTypes(results: PagedResults<CredentialType>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listCredentialTypes',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<CredentialType>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listCredentialTypes`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'CredentialType');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listOrgCredentials(results: PagedResults<OrgCredential>, organizationId: string): Promise<void> {
  const _body = {
      organizationId,
  };
  return this.client.put(
    '/listOrgCredentials',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<OrgCredential>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listOrgCredentials`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'OrgCredential');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}
