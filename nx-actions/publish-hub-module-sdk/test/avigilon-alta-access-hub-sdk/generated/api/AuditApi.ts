/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { AuditLogEntry } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace AuditApi {
}

/**
 * AuditApi - interface
 * @export
 * @interface AuditApi
 */
export interface AuditApi {
  /**
   * 
   * @summary Retrieves audit logs for the organization
   * @param {string} organizationId Unique identifier for the organization
   * @param {string} [filter] Filter criteria for audit logs (e.g., \&quot;timestamp:(1753282000--1753284000)\&quot;)
   * @param {string} [options] Search options (e.g., \&quot;searchId:123-456-0\&quot;)
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listAuditLogs('organizationId', 'filter', 'options', 1, 1);
   * ```
   * @openapi operation=listAuditLogs path=/organizations/{organizationId}/reports/auditLogs/ui method=GET
   * @memberof AuditApi
   */
  listAuditLogs(organizationId: string, filter?: string, options?: string, pageNumber?: number, pageSize?: number): Promise<PagedResults<AuditLogEntry>>;

}

/**
 * AuditProducerApi - interface
 * Producer interface for Audit
 * @export
 * @interface AuditProducerApi
 */
export interface AuditProducerApi {
  /**
  * 
  * @summary Retrieves audit logs for the organization
  * @param {string} organizationId Unique identifier for the organization
  * @param {string} [filter] Filter criteria for audit logs (e.g., \&quot;timestamp:(1753282000--1753284000)\&quot;)
  * @param {string} [options] Search options (e.g., \&quot;searchId:123-456-0\&quot;)
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listAuditLogs path=/organizations/{organizationId}/reports/auditLogs/ui method=GET
  * @memberof Audit
  */
  listAuditLogs(results: PagedResults<AuditLogEntry>, organizationId: string, filter?: string, options?: string): Promise<void>;
}



export class AuditApiMappingImpl implements AuditApi {
  private producer: AuditProducerApi;
  constructor(producer: AuditProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Retrieves audit logs for the organization
   * @param organizationId Unique identifier for the organization
   * @param filter Filter criteria for audit logs (e.g., \&quot;timestamp:(1753282000--1753284000)\&quot;)
   * @param options Search options (e.g., \&quot;searchId:123-456-0\&quot;)
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listAuditLogs path=/organizations/{organizationId}/reports/auditLogs/ui method=GET
   */
  public async listAuditLogs(organizationId: string, filter?: string, options?: string, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<AuditLogEntry>> {
    // verify required parameter 'organizationId' is not null or undefined
    if (organizationId === null || organizationId === undefined || organizationId === '') {
      throw new ParameterRequiredError('organizationId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<AuditLogEntry>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listAuditLogs(bag, organizationId, filter, options);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}


export class AuditApiHubImpl implements AuditProducerApi {
  private client: AxiosInstance;

  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async listAuditLogs(results: PagedResults<AuditLogEntry>, organizationId: string, filter?: string, options?: string): Promise<void> {
  const _body = {
      organizationId,filter,options,
  };
  return this.client.put(
    '/listAuditLogs',
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      results.items = ObjectSerializer.deserialize(response.data, 'Array<AuditLogEntry>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/listAuditLogs`);
      results.httpMethod = HttpMethod.Put;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'AuditLogEntry');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}
